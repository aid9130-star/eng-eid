# 🚀 دليل رفع وتشغيل "منصة التفوق" على الاستضافة

تم فحص المشروع وتصحيح كافة الأخطاء البرمجية وإعدادات الاستضافة ليعمل المشروع بسلاسة **100%** على جميع أنواع الاستضافات سواء كانت استضافة عادية (cPanel / Hostinger / Apache) أو استضافة سحابية سريعة (Netlify / Vercel) أو خادم Node.js كامل مع قاعدة بيانات PostgreSQL.

---

## 🛠️ أبرز الأخطاء التي تم تصحيحها في المشروع:

1. **إصلاح خطأ إصدار TypeScript في `package.json`**:
   - كان مكتوباً `typescript: ^7.0.2` وهو إصدار غير موجود نهائياً على npm، مما كان يتسبب في فشل أمر التثبيت `npm install` فوراً على أي استضافة بخطأ `ETARGET`. تم تصحيحه إلى `^5.7.2`.
2. **إصلاح منفذ الخادم `PORT` في `server.ts`**:
   - كان المنفذ ثابتاً `3000`، مما يمنع تشغيل الخادم على خدمات مثل (Render, Railway, Heroku, Cloud Run) لأنها تتطلب قراءة المنفذ من المتغير `process.env.PORT`. تم تعديله ليدعم `process.env.PORT || 3000`.
3. **دعم قواعد بيانات السحابة (Neon, Supabase, Render Postgres)**:
   - تم تحديث اتصال قاعدة البيانات `src/db/index.ts` ليدعم متغير `DATABASE_URL` أو `POSTGRES_URL` مع تفعيل وضع التشفير الآمن `SSL: { rejectUnauthorized: false }`.
4. **دعم المسارات النسبية في Vite**:
   - تم تغيير `base: '/'` إلى `base: './'` لضمان عمل الموقع حتى لو تم رفعه داخل مجلد فرعي (مثل: `mysite.com/platform/`) دون انكسار مسارات الصور وملفات الـ JavaScript والـ CSS.
5. **إضافة ملفات التوجيه للاستضافات (SPA Routing)**:
   - إنشاء ملف `public/.htaccess` لاستضافات cPanel و Apache و LiteSpeed حتى لا تظهر صفحة 404 عند تحديث الصفحة أو الدخول لروابط داخلية.
   - إنشاء ملف `public/_redirects` لخدمات Netlify و Cloudflare Pages.
   - إنشاء ملف `vercel.json` لخدمة Vercel.
6. **النظام الهجين الذكي (Hybrid Offline / Client DB)**:
   - تم بناء نظام ذكي مدمج؛ فإذا رُفع الموقع على استضافة ثابتة (بدون خادم Node.js أو قاعدة بيانات PostgreSQL)، يستمر الموقع في العمل بكامل كفاءته عبر التخزين المحلي التلقائي `LocalStorage`، فيستطيع الطلاب تسجيل الدخول، وحل الامتحانات، وتصحيحها فورياً، وتستطيع كمعلم الدخول برمز PIN وإدارة الأكواد وإضافة الدروس والاختبارات!

---

## 🌐 طرق رفع الموقع على الاستضافة:

### 1️⃣ الطريقة الأولى: الرفع على استضافة cPanel / Hostinger (الأكثر شيوعاً)

1. افتح موجه الأوامر (Terminal) في مجلد المشروع ونفّذ أمر البناء:
   ```bash
   npm run build:client
   ```
   *(أو `npm run build`)*
2. سيتم إنشاء مجلد جديد باسم **`dist`** يحتوي على ملفات الموقع المترجمة والجاهزة ومعها ملف `.htaccess`.
3. افتح لوحة تحكم الاستضافة **cPanel** واختر **مدير الملفات (File Manager)**.
4. ادخل إلى مجلد **`public_html`** (أو مجلد موقعك الفرعي).
5. اضغط على **Upload (رفع)** وارفع محتويات مجلد `dist` كاملة إلى `public_html`.
6. افتح رابط موقعك، وستجد المنصة تعمل بكامل طاقتها وتصميمها!

---

### 2️⃣ الطريقة الثانية: الرفع المجاني السريع على Netlify أو Vercel

#### أ) الرفع على Netlify (بدون كتابة أوامر):
1. نفّذ أمر البناء `npm run build:client`.
2. ادخل إلى [Netlify](https://app.netlify.com).
3. في قسم **Sites**، اسحب مجلد **`dist`** وأفلته في المربع المخصص للرفع (Drag & Drop).
4. ستحصل على رابط مباشر مجاني للموقع فوراً مع شهادة أمان SSL تلقائية.

#### ب) الرفع على Vercel:
1. ارفع الكود إلى GitHub ثم اربط المستودع مع [Vercel](https://vercel.com).
2. سيتعرف Vercel على إعدادات Vite تلقائياً (Build command: `vite build`, Output directory: `dist`).
3. اضغط **Deploy**.

---

### 3️⃣ الطريقة الثالثة: الرفع كخادم كامل Node.js + PostgreSQL (على Render أو Railway)

إذا أردت تشغيل الموقع مع الخادم الخلفي `server.ts` وقاعدة بيانات PostgreSQL مشتركة بين جميع المستخدمين:

1. أنشئ قاعدة بيانات PostgreSQL مجانية على [Neon.tech](https://neon.tech) أو [Supabase](https://supabase.com).
2. انسخ رابط الاتصال (`DATABASE_URL`).
3. اربط مستودع GitHub مع [Render.com](https://render.com) كـ **Web Service**:
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - في خانة **Environment Variables**، أضف:
     - `DATABASE_URL`: رابط الاتصال بقاعدة البيانات.
     - `ADMIN_PIN`: كلمة المرور الخاصة بك كمعلم (الافتراضية: `emam2025`).
4. اضغط **Create Web Service**، وسيقوم Render ببناء الخادم والواجهة معاً وتشغيل المنصة تلقائياً.

---

## 🔑 بيانات الدخول الافتراضية الجاهزة للتجربة:

- **دخول المعلم (لوحة التحكم):**
  - اضغط على "دخول المعلم" في أعلى الصفحة.
  - كلمة المرور / رمز الدخول السري: `emam2025` أو `2025`.
- **أكواد تجريبية جاهزة لتسجيل دخول الطلاب:**
  - `TOP-2025-A1`
  - `TOP-2025-B2`
  - `EMAM-ENG-01`
  - `EMAM-ENG-02`
  - `VIP-EXAM-77`
  *(كما يمكنك كمعلم توليد مئات الأكواد الجديدة بضغطة زر واحدة من لوحة التحكم).*
